apiVersion: v1
kind: Service
metadata:
  name: landguard-backend
  namespace: landguard
  labels:
    app: landguard-backend
spec:
  selector:
    app: landguard-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
